from pathlib import Path
import pandas as pd
out=Path('/home/hy/wpt_agv_opportunity_charging/results_v3_figures')
base=pd.read_csv(out/'plot_data_Figure1_base_summary.csv')
chal=pd.read_csv(out/'plot_data_Figure1_challenge_summary.csv')
trade=pd.read_csv(out/'plot_data_Figure2_tradeoff_summary.csv')
dec=pd.read_csv(out/'plot_data_Figure3_decision_summary.csv')
feat=pd.read_csv(out/'plot_data_Figure3_feature_std.csv')
adv=pd.read_csv(out/'plot_data_Figure4_advantage_heatmap.csv')

def val(df, st, col): return float(df.loc[df.strategy==st,col].iloc[0])
fig1_base_delay = {s: val(base,s,'mean_delay_mean') for s in ['C1','C2','C3','C4']}
fig1_chal_delay = {s: val(chal,s,'mean_delay_mean') for s in ['C1','C2','C3','C4']}
fig1_chal_urgent = {s: val(chal,s,'urgent_on_time_rate_mean') for s in ['C1','C2','C3','C4']}
contention=float(dec.loc[dec.metric=='contention_events','mean'].iloc[0]); diff=float(dec.loc[dec.metric=='different_decision_rate','mean'].iloc[0])
# Figure4 highlight regions
adv_sorted=adv.sort_values('delay_improvement_pct', ascending=False)
best=adv_sorted.iloc[0]
near_zero=adv[(adv.delay_improvement_pct.abs()<1.0)].head(3)

captions=f'''# Captions for the four selected main figures\n\n## Figure 1. Base-case saturation and challenge-case separation.\nFigure 1 compares the Original Base Case and the Scheduling Challenge Case using 50 common-random-number replications. In the Base Case, C2, C3, and C4 are practically indistinguishable in mean delay (all about {fig1_base_delay['C2']:.2f} min), confirming that opportunity charging itself dominates when charging resources and task homogeneity leave little room for priority decisions. In the Challenge Case, the same strategies separate: C2 gives the lowest delay ({fig1_chal_delay['C2']:.2f} min), while C3 and C4 provide much higher urgent-task on-time completion ({fig1_chal_urgent['C3']:.1f}% and {fig1_chal_urgent['C4']:.1f}%). Error bars indicate 95% confidence intervals of the replication mean.\n\n**Why this figure matters.** This is the opening story figure: it prevents overclaiming by showing that C4 is not needed in the easy Base Case, then motivates why the Challenge Case is necessary. It also makes C1's threshold-charging limitation visible without requiring many separate plots.\n\n## Figure 2. Challenge-case trade-off between delay, urgent service, and WPT loss.\nFigure 2 plots each strategy in the Challenge Case with mean task delay on the x-axis and urgent-task on-time completion on the y-axis. Point color and size encode WPT energy loss, selected because it directly represents the energy-side consequence of different charging choices while avoiding a cluttered 3D plot. C2 occupies the low-delay region, C3 maximizes urgent-task on-time completion, and C4 lies between them while reducing WPT loss relative to C3. C1 is dominated or nearly dominated because it has both high delay and low urgent-service performance.\n\n**Why this figure matters.** This figure summarizes the main trade-off instead of presenting isolated metrics. It supports the interpretation that C4 is a compromise scheduler, not a universally best scheduler.\n\n## Figure 3. Decision diagnostics explaining why C4 differs from C3.\nFigure 3(a) shows that the Challenge Case generated substantial charging contention, with C4 observing an average of {contention:.1f} contention events per replication. C4 selected a different AGV than the C3 low-SOC rule in {diff:.1f}% of contention events, confirming that the proposed priority score changed actual scheduling decisions. Figure 3(b) reports the standard deviation of normalized C4 features; E_next, eta_WPT, 1-SOC, and T_idle all have nonzero variance, while D is smaller but nonzero under the urgent-deadline setting. Together, these diagnostics show that C4 had both the opportunity and the information needed to differ from C3.\n\n**Why this figure matters.** Performance differences alone do not prove that the scheduler is doing something different. This figure links the observed outcomes to mechanism: contention plus feature variance plus different decisions.\n\n## Figure 4. C4 advantage region across workload, pad count, and charging power.\nFigure 4 maps the stress-grid result as C4 delay improvement over C3, computed as (C3 delay − C4 delay) / C3 delay × 100. Positive cells indicate conditions where C4 reduced mean delay relative to C3, and the parenthesized annotations show the accompanying urgent-task on-time difference in percentage points. The strongest C4 delay gains occur in resource-scarce and heterogeneous operating regions, for example workload={int(best.workload)} tasks/h, pads={int(best.pads)}, power={int(best.power)} kW, where the delay improvement is {best.delay_improvement_pct:.1f}%. Where infrastructure is more sufficient or strategies already converge, the additional C4 benefit becomes small.\n\n**Why this figure matters.** This is the design implication figure. It converts many stress-grid runs into a compact answer to “when is the complex scheduler worth using?”\n'''
(out/'CAPTIONS.md').write_text(captions,encoding='utf-8')
selection='''# Main Figure Selection for V3\n\n## Data sources used\n\nThe four main figures were regenerated from V2 CSV outputs without changing the underlying simulation results. The inputs were:\n\n- `results_v2/base_case_runs.csv` for Original Base Case strategy metrics.\n- `results_v2/challenge_runs.csv` for Primary Challenge strategy metrics.\n- `results_v2/stress_grid.csv` for workload × pad × power design-region analysis.\n- `results_v2/priority_features.csv` for aggregated C4 feature variance diagnostics.\n- `results_v2/priority_features_raw_sample.csv` only for traceability of feature distributions; the main plotted Figure 3(b) uses aggregated standard deviations.\n- `results_v2/decision_diagnostics.csv` for contention-event and C4-vs-C3 different-decision diagnostics.\n\nAll bars and points represent means across 50 replications. Where error bars are shown, they are 95% confidence intervals of the replication mean, which is more appropriate than raw standard deviation for publication figures focused on comparing expected strategy performance.\n\n## Why exactly these four figures were selected\n\nThe requested paper/presentation story has four logical claims, so the main figure set was limited to one figure per claim:\n\n1. **Figure 1 — Problem motivation and base/challenge contrast.** It shows that the Base Case mainly proves the value of opportunity charging, while the Challenge Case creates meaningful separation among C2, C3, and C4. This prevents the paper from implying that C4 must always dominate.\n2. **Figure 2 — Trade-off summary.** It condenses the Challenge Case into a Pareto-style view: C2 is delay-oriented, C3 is urgent-deadline-oriented, and C4 is a compromise with energy implications.\n3. **Figure 3 — Mechanistic diagnosis.** It demonstrates that C4 really made different decisions from C3 and that the score inputs had nonzero variance. This supports the scheduler contribution rather than just reporting output metrics.\n4. **Figure 4 — Operating-region/design implication.** It shows when C4 is useful across workload, pad count, and power, supporting the “resource scarcity + task heterogeneity” conclusion.\n\nTogether these four figures form a compact story: Base Case convergence → Challenge trade-off → C4 decision mechanism → C4 advantage region.\n\n## Figures intentionally not selected as main figures\n\nSeveral V2 figures were not carried into the main paper set:\n\n- Separate one-metric bar charts for completion rate, low-SOC stoppage, charging waiting time, and pad utilization were omitted because they fragment the story and repeat information captured in Figures 1–2.\n- Individual workload-vs-delay, workload-vs-urgent-rate, pad-count, and power line plots were replaced by Figure 4, which integrates the full stress grid more efficiently.\n- Full feature-distribution plots and raw decision-event plots were reduced to Figure 3's diagnostic panels because the paper needs mechanism evidence, not every diagnostic trace.\n- Large task-level or AGV-level trajectory plots were omitted from the main set because they are useful for validation but less central to the proposed scheduler argument.\n\n## Is the four-figure main story sufficient?\n\nYes. The four selected figures are sufficient for the main paper/presentation because each directly supports one of the four core messages and avoids redundant metric-by-metric plotting. The set also makes the negative/conditional finding clear: C4 is not universally superior, and its value appears when contention and heterogeneity exist.\n\n## Supplementary figure candidates, if absolutely needed\n\nKeep the main figure count at four. If supplementary material is allowed, only these two additions are recommended:\n\n1. **Supplementary Figure S1 — Full ablation matrix.** Equal vs variable distance, 0% vs 20% urgent tasks, and fixed vs variable eta, shown as compact grouped bars for delay, urgent on-time, and WPT loss. This would support feature-contribution claims without overloading the main paper.\n2. **Supplementary Figure S2 — SOC and pad-utilization validation traces.** A representative Challenge Case run showing SOC trajectories and pad occupancy. This would reassure readers that the finite-horizon simulation and charging quantum dynamics are physically reasonable.\n'''
(out/'FIGURE_SELECTION.md').write_text(selection,encoding='utf-8')
print('Wrote CAPTIONS.md and FIGURE_SELECTION.md')
